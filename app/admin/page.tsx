import AdminDashboard from "@/components/admin/AdminDashboard";

export const metadata = {
  title: "Admin & Operations — NEST Solutions",
};

export default function AdminPage() {
  return <AdminDashboard />;
}

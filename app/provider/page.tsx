import ProviderApp from "@/components/provider/ProviderApp";

export const metadata = {
  title: "Provider app — NEST Solutions",
};

export default function ProviderPage() {
  return <ProviderApp />;
}
